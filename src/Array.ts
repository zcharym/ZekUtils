export default class ZArray {
    /**
     * check whether it is an array
     * @param {Object} arr  
     * @returns boolean
     */
    isArray(arr: any): Boolean {
        return typeof arr === 'Array'
    }
}
